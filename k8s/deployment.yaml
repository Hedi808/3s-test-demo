# Generated by CloudMind
apiVersion: apps/v1
kind: Deployment
metadata:
  name: 3s-test-demo-app
  namespace: cloudmind-staging
  labels:
    app.kubernetes.io/name: 3s-test-demo
    app.kubernetes.io/component: app
    app.kubernetes.io/managed-by: cloudmind
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: 3s-test-demo
      app.kubernetes.io/component: app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: 3s-test-demo
        app.kubernetes.io/component: app
    spec:
      containers:
        - name: app
          image: ghcr.io/Hedi808/3s-test-demo-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: 3s-test-demo-config
            - secretRef:
                name: 3s-test-demo-secret
                optional: true
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

